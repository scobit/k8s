# для того, чтобы вывести под из rc необходимо изменить его метку. Тогда он выйдет из rc и rc создаст новый вместо выведенного
kubectl label pod PodName LabelName=LabelValue --overwrite

# Селекторы rc определяют область действия контроллера

