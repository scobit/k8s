# документация по установке
https://kubernetes.io/ru/docs/tasks/tools/install-minikube/

# help
minikube --help
minikube start --help


# check version
minikube version

# start existing or create and start new cluster
minikube start

# start with specific cluster name
minikube start -p MyClusterName

# create new cluster with specific parameters
minikube start --cpus=4 --memory=8gb | --memory=8000mb --disk-size=25gb

# check status
kubectl status

# stop cluster
minikube stop

# delete cluster
minikube delete

# iso file minikube destination 
~/.minikube

# login into minikube
# login root / empty password
# login docker / tcuser
# or empty password and some login above
# can check images in minikube docker images
minikube ssh






